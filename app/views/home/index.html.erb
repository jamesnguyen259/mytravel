<% provide(:title, "Home") %>

<% content_for :content do%>
    <div id="colorlib-main">
		<div class="colorlib-blog">
			<div class="container-wrap">
				<div class="row">
					<div class="col-md-9">
						<div class="content-wrap">
							<% @place_reviews.each do |place| %>
								<article class="animate-box">
									<div class="blog-img" style="background-image: url(/assets/<%= place.place_pictures.first.picture %>);"></div>
									<div class="desc">
										<div class="meta">
											<p>
												<span><%= place.address.name %></span>
												<span><%= place.user.name %></span>
												<span><%= place.created_at.strftime("%d %b %Y") %></span>
											</p>
										</div>
										<h2><%= link_to place.place_name, place_path(place.id)  %></h2>
										<p><%= place.review_content[0..250] %>...</p>
										<p><%= link_to place_path(place.id), class:"btn btn-primary with-arrow" do %>
											Read More <i class="fa fa-angle-double-right"></i>
										<% end %></p>
									</div>
								</article>
							<% end %>
							<div class="row">
								<div class="col-md-12 animate-box" data-animate-effect="fadeInLeft">
									<div class="digg_pagination">
										<%= will_paginate @place_reviews %>
									</div>
								</div>
							</div>
						</div>
					</div>
					<div class="col-md-3 sticky-parent">
						<div class="sidebar" id="sticky_item">
							<div class="side animate-box">
								<div class="form-group">
									<input type="text" class="form-control" id="search" placeholder="Enter to search...">
									<button type="submit" class="btn submit btn-primary"><i class="fa fa-search"></i></button>
								</div>
							</div>
							<div class="side animate-box">
								<h2 class="sidebar-heading">Popular address</h2>
								<p>
									<ul class="category">
										<% @popular_address.each do |address| %>
											<li>
												<%= link_to address_path(address.id) do %>
												  <i class="fa fa-star"></i><%= address.name %>
												<% end %>
											</li>
										<% end %>
									</ul>
								</p>
							</div>
							<div class="side animate-box">
								<h2 class="sidebar-heading">Recent Blog</h2>
								<% @recent_reviews.each do |review| %>
									<div class="f-blog">
										<a href="<%= place_path(review.id) %>" class="blog-img" style="background-image: url(/assets/<%= review.place_pictures.first.picture %>);">
										</a>
										<div class="desc">
											<h3><%= link_to review.place_name, place_path(review.id) %></h3>
											<p class="admin"><span><%= review.created_at.strftime("%d %b %Y") %></span></p>
										</div>
									</div>
								<% end %>
							</div>
							<div class="side animate-box">
								<h2 class="sidebar-heading">Most viewed</h2>
								<% @most_viewed.each do |review| %>
									<div class="f-blog">
										<a href="<%= place_path(review.id) %>" class="blog-img" style="background-image: url(/assets/<%= review.place_pictures.first.picture %>);">
										</a>
										<div class="desc">
											<h3><%= link_to review.place_name, place_path(review.id) %></h3>
											<p class="admin"><span><%= review.created_at.strftime("%d %b %Y") %></span></p>
										</div>
									</div>
								<% end %>
							</div>
							<div class="side animate-box">
								<div class="form-group">
									<input type="text" class="form-control form-email text-center" id="email" placeholder="Enter your email">
									<button type="submit" class="btn btn-primary btn-subscribe">Subscribe</button>
								</div>
							</div>
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>
<% end %>
